# hosting-temp

# Run bootstrap.sh on Cloudshell in your prefereed region and save credentials
# Create SA_JSON as secret in Github Actions. Required JSON related to ./.github/schemas/sa.json
# Create SECRETS_JSON as secret in Github Actions. Required JSON related to ./.github/schemas/secrets.json
# Paste VARS_JSON as secret in Github Actions. Required JSON related to ./.github/schemas/vars_schema.json
# Create MODULES_JSON as secret in Github Actions. Required JSON related to ./.github/schemas/modules.json
# Run playbook